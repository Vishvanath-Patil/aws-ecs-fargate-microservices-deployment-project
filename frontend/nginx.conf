server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
    # for local testing  with docker compose uncomment the below lines 

    # location /api/auth/ {
    #     proxy_pass http://auth-service:8000/;
    # }

    # location /api/user/ {
    #     proxy_pass http://user-service:8000/;
    # }

    # location /api/post/ {
    #     proxy_pass http://post-service:8000/;
    # }

    # location /api/like/ {
    #     proxy_pass http://like-service:8000/;
    # }
}
